@startuml

left header
<font color=000 size=18><b>Package:</b> AD_1.1

<font color=000 size=16><b>ID:</b> UC_2.1
<font color=000 size=16><b>Назва:</b> Переглянути метадані звіту
<font color=000 size=16><b>Учасники:</b> Користувач, Система
<font color=000 size=16><b>Передумови:</b> Відсутні
<font color=000 size=16><b>Результат:</b> Перехід на звіт
<font color=000 size=16><b>Виключні ситуації:</b>
<font color=000 size=16> EX_2.1.1 Звіт відсутній

<font color=000 size=16><b>Основний сценарій:</b>

end header

|Користувач|
start
: Натискає на звіт у списку;
|Система|
: Отримує ідентифікатор звіту
та здійснює пошук
конфігурації звіту;

note right #aaffaa

Система генерує ідентифікатор
звіту під час його створення та
зберігає разом з конфігурацією звіту
(див. UC_5).

<font color=red><b> Можливо EX_2.1.1.

end note

: Здійснює вибірку пов'язаних
із звітом пошукових тегів;
note right #aaffaa

Автор або редактор звіту визначає
зв'язки з тегами під час створення або
редагування звіту

end note

: Здійснює вибірку інформації
про користовачів з правами
автора або редактора відносно
вибраного звіту;
note right #aaffaa

Система визначає роль автора для користувача,
який створює звіт, під час створення звіту.
Роль редактора для користувачів
визначається автором під час створення або
редагування звіту.

end note

: Формує загальний список
звітів за вибраною в попередньому
пункті множиною користувачів;

: Генерує <b>картку метаданих звіту</b>;
note right #aaffaa
<b> Картка метаданих звіту повинна містити:</b>

Посилання на виклик звіту з його назвою.

Короткий опис звіту з його конфігурації.

Дати створення та останнього оновлення звіту.

Інформацію про авторів звіту
( роль, ім'я, прізвище, аватарка або картинка
за замовченням ). Інформація про авторів звіту
відображується у вигляді посилань, натискання на
які викликає фільтрацію списку звітів.

Інформацію про звіти команди розробників у
вигляді посилань (не більше 5, оновлених
останнім часом). Натискання на посилання звіту
викликає UC_2.1 "Переглянути метадані звіту"  

Інформацію про пошукові теги, пов'язані із звітом,
у вигляді посилань, що викликають фільтрацію списку
звітів

end note
|Користувач|
stop;

right footer


Аналітичний портал. Модель прецедентів.
НТУУ КПІ ім.І.Сікорського
Киів-2020
end footer